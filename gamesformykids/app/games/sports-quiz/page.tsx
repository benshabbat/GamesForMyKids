import SportsQuizGameClient from './SportsQuizGameClient';

export const metadata = { title: 'חידון ספורט', description: 'שאלות על עולם הספורט!' };

export default function SportsQuizPage() {
  return <SportsQuizGameClient />;
}
